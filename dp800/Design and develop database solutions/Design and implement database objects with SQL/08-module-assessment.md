# Module assessment

200 XP
10 Minutes

This assessment evaluates your understanding of the module. Unlike before, you won't get feedback on individual answers—just whether they're right or wrong. This is meant to measure what you've learned. Take time to review the module materials before starting.

> AI-generated content
>
> The questions and answer choices in this module assessment were generated using AI and reviewed by a human author.

**1. A company wants to ensure each employee ID is unique across multiple tables in their database. Which feature should be used to accomplish this?**

- Sequence objects
- Identity columns
- Unique constraints

**2. Which SQL platform is best suited for scenarios requiring automatic scaling of compute resources and minimal infrastructure management?**

- Azure SQL Managed Instance
- SQL Server on Azure Virtual Machines
- Azure SQL Database - Serverless tier

**3. In a SQL Database in Microsoft Fabric, which index type should be used for a large table primarily accessed through analytical queries?**

- Nonclustered index
- Rowstore clustered index
- Columnstore index

**4. When designing a table for a high-throughput transactional application on Azure SQL Managed Instance, which table type would be most suitable to minimize latency?**

- External table
- In-memory optimized table
- Graph table

**5. Your company is expanding its database to handle a multi-terabyte dataset with frequent read operations. Which SQL Server feature would best support this requirement?**

- Serverless compute tier
- Hyperscale service tier
- In-memory OLTP

**6. Which SQL platform would be most appropriate for an organization requiring full OS-level access and specific SQL Server configurations not available in PaaS offerings?**

- Azure SQL Managed Instance
- Azure SQL Database
- SQL Server on Azure Virtual Machines

**7. Which SQL platform provides built-in high availability with a 99.99% uptime Service Level Agreement (SLA)?**

- Azure SQL Database
- Azure SQL Managed Instance
- SQL Server on Azure Virtual Machines

**8. What is a primary benefit of using JSON columns in SQL databases when dealing with variable data structures?**

- They automatically enforce data integrity constraints.
- They enable flexible schemas without altering the table structure.
- They allow storing large binary data efficiently.

**9. To ensure referential integrity across multiple tables with shared keys, which constraint should be implemented?**

- Check constraint
- Foreign key constraint
- Unique constraint
