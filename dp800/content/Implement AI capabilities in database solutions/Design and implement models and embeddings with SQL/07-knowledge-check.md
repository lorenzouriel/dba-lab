# Knowledge check

Completed

- 5 minutes

## Check your knowledge

**1. What is an external model in Azure SQL Database?**

- A machine learning model that runs inside the SQL Server engine
- A database object that stores metadata about an AI model endpoint
- A copy of an AI model downloaded to the database server

**2. What is the purpose of chunking in embedding design?**

- To compress vectors for faster storage
- To divide large text into smaller segments that fit within token limits
- To encrypt sensitive data before embedding

**3. What data type does Azure SQL Database use to store embeddings?**

- NVARCHAR(MAX)
- VECTOR
- JSON

**4. In RAG patterns, what role do embeddings play?**

- They encrypt data before sending it to the model
- They represent data as vectors for semantic similarity comparison
- They translate queries into multiple languages
